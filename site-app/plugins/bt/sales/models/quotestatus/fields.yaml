# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
    name:
        label: Status Name
        disabled: false
    action:
        label: Action Name
        disabled: false
    candelete:
        label: Can Delete?
        type: switch
    emailgroup:
      label: Email User Group
      select: name
      type: relation
      placeholder: -- select a status --
    