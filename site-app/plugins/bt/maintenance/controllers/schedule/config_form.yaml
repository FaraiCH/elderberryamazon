# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Schedule

# Model Form Field configuration
form: $/bt/maintenance/models/schedule/fields.yaml

# Model Class name
modelClass: Bt\Maintenance\Models\Schedule

# Default redirect location
defaultRedirect: bt/maintenance/schedule

# Create page
create:
    title: Create Schedule
    redirect: bt/maintenance/schedule/update/:id
    redirectClose: bt/maintenance/schedule

# Update page
update:
    title: Edit Schedule
    redirect: bt/maintenance/schedule
    redirectClose: bt/maintenance/schedule

# Preview page
preview:
    title: Preview Schedule
