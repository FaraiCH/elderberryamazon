# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    start_date:
        label: Start Date
    end_date:
        label: Start Date
    line:
        label: Elderberry Amazon Machine
        relation: line
        select: name


    off_peak_kwh:
        label: Off Peak KWH

    off_peak_amount:
        label: Off Peak Amount



    standard_kwh:
        label: Standard KWH

    standard_amount:
        label: Standard Amount
        format: "%.1f kg"


    on_peak_kwh:
        label: On Peak KWH

    on_peak_amount:
        label: On Peak Amount

        format: "%.1f kg"


    max_demand_amount:
        label: Max Demand Amount

        format: "%.1f kg"

    fixed_charge_amount:
        label: Fixed Charge

        format: "%.1f kg"

    network_access_amount:
        label: Network Access
        format: "%.1f kg"

